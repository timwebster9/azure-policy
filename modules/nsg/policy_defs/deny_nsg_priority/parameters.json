{
    "effect": {
        "type": "String",
        "defaultValue": "Deny",
        "allowedValues": [
            "Audit",
            "Deny",
            "Disabled"
        ],
        "metadata": {
            "displayName": "Effect",
            "description": "The effect determines what happens when the policy rule is evaluated to match"
        }
    },
    "priorityThreshold": {
        "type": "Integer",
        "defaultValue": 200,
        "metadata": {
            "displayName": "Priority Threshold",
            "description": "The priorty below which the policy takes effect.  E.g. a rule with a priority < 200 will result in a deny effect."
        }
    }
}